<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 배경은 투명 -->
    <solid android:color="@android:color/transparent"/>

    <!-- 모서리 둥글게 (선택사항) -->
    <corners android:radius="12dp"/>

    <!-- 테두리만 파란색 -->
    <stroke
        android:width="3dp"
        android:color="#00B6F1"/>
</shape>
